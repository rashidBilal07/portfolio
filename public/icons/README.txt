App icons, named <slug>.png at 512x512.

These were downloaded from each app's Google Play listing (the og:image,
requested at 512x512). To add another app, drop its icon here named to match
the project's `slug` in data/projects.ts.

Until a file exists, the site renders a placeholder showing the path it expects.
